version: "3"
services:
  web:
    # replace username/repo:tag with your name and image details
    image: jeffersonsilveira/playlist-compare
    ports:
      - "4000:80"
    networks:
      - webnet
networks:
  webnet:
